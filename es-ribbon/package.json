{
  "name": "bgo-ui",
  "version": "1.0.0",
  "description": "Blue Green Optimizer user interface \"hosting\" page and docker compose.",
  "main": "index.js",
  "scripts": {
    "build": "lerna exec -- npm run build",
    "clean": "lerna exec -- npm run clean",
    "dev": "lerna exec -- npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.moesol.com:dfntc/bgo-ui.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {}
}
