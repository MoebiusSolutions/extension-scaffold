import React from 'react'

export const MyPanel: React.FC<{}> = () => {
    return <div>MyPanel - snowpack - with a whole lot of text so that if a panel is over this Panel
        you can still see that something is here.
    </div>
}
